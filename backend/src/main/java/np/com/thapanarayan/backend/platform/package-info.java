/**
 * {@code platform} module — shared kernel: error model, pagination, NPT time/calendar utilities,
 * OpenAPI and object-store configuration. Every other module may depend on
 * {@code platform.api}; this is the one module whose API is intended for universal reuse.
 */
package np.com.thapanarayan.backend.platform;
