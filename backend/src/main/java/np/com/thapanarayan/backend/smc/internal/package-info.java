/**
 * smc module — internal package.
 *
 * <p>Smart Money Concepts detection over daily candles (read-only via
 * {@code marketdata.api}).</p>
 *
 * <p>The {@code api} package is the module's published surface; {@code internal} is
 * implementation detail that no other module may reference (enforced by ArchUnit).</p>
 */
package np.com.thapanarayan.backend.smc.internal;
